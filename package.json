{
  "engines": {
    "node": ">= 8.1.4",
    "yarn": ">1.0.1"
  },
  "scripts": {
    "format": "prettier --write **/*.js package.json",
    "start": "nodemon src/server --watch src/server",
    "start-p": "cross-env NODE_ENV=production node src/server",
    "build": "next build src",
    "heroku-postbuild": "yarn build",
    "heroku-start": "node src/server"
  },
  "dependencies": {
    "koa": "^2.4.1",
    "koa-router": "^7.4.0",
    "koa-useragent": "^1.0.0",
    "next": "^4.2.3",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "devDependencies": {
    "cross-env": "^5.1.3",
    "nodemon": "^1.14.12",
    "npm-run-all": "^4.1.2"
  }
}
